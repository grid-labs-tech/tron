# Tron Production Environment Configuration
# Copy this file to .env and configure the values

# =============================================================================
# Required Variables
# =============================================================================

# Database password (REQUIRED - use a strong password)
DB_PASSWORD=your-secure-database-password

# API Secret Key (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-minimum-32-characters-here

# =============================================================================
# Optional Variables
# =============================================================================

# Database user (default: tron)
DB_USER=tron

# Image tags (default: latest)
# Use specific versions for production stability
API_IMAGE_TAG=0.3.0
PORTAL_IMAGE_TAG=0.3.0

# CORS configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=https://your-domain.com

# API URL for Portal (use /api when behind nginx proxy)
API_URL=/api

# =============================================================================
# Nginx Configuration
# =============================================================================

# Server name (use _ for catch-all or your domain)
NGINX_SERVER_NAME=_

# Port mappings
HTTP_PORT=80
HTTPS_PORT=443

# Performance tuning
NGINX_CLIENT_MAX_BODY_SIZE=100M
NGINX_PROXY_CONNECT_TIMEOUT=60s
NGINX_PROXY_SEND_TIMEOUT=60s
NGINX_PROXY_READ_TIMEOUT=60s

# SSL protocols (default: TLSv1.2 TLSv1.3)
NGINX_SSL_PROTOCOLS=TLSv1.2 TLSv1.3

# =============================================================================
# SSL/HTTPS Configuration (required for --profile ssl)
# =============================================================================

# Your domain name (e.g., tron.example.com)
DOMAIN=your-domain.com

# Email for Let's Encrypt notifications
CERTBOT_EMAIL=admin@your-domain.com

# Certificate renewal interval (default: 60d = 60 days)
# Let's Encrypt certificates are valid for 90 days
# Recommended: 60d (renew 30 days before expiry)
CERTBOT_RENEWAL_INTERVAL=60d

# =============================================================================
# Custom SSL Certificates (optional - alternative to Let's Encrypt)
# =============================================================================

# If you have your own SSL certificates, set these paths:
# NGINX_SSL_CERT_PATH=/etc/ssl/certs/your-cert.pem
# NGINX_SSL_KEY_PATH=/etc/ssl/private/your-key.pem
